# D_Flip-Flop
A simple 16-bit D flip-flop design and testbench written in SystemVerilog.  This repository contains a register module that captures 16-bit input data (D) on the rising edge of a clock (clk) and outputs the stored value (Q). A corresponding testbench verifies correct functionality by applying stimulus vectors and displaying simulation results.
